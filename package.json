{
  "name": "loan-management-app",
  "version": "1.0.0",
  "description": "for managing loans by uploading images of payment of reciepts and when they are due to be payed ",
  "license": "ISC",
  "author": "Jude Nwadiogor",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --env-file=.env --watch src/server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1",
    "multer": "^2.0.2",
    "tesseract.js": "^7.0.0"
  }
}
